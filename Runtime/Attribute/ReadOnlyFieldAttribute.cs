using UnityEngine;

namespace AceLand.Library.Attribute
{
    public class ReadOnlyFieldAttribute : PropertyAttribute
    {
        // This is just a marker attribute to indicate that a property should be read-only.
    }
}
